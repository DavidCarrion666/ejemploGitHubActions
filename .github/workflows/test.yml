name: Ejemplo CI/CD

on:
  push:

jobs:
  inicio:
    runs-on: windows-latest
    steps:
      - name: Mostrar texto
        run: echo "Paso para colnar repositorio a github actions"
      - name: Checkout
        uses: actions/checkout@v4.2.2

  build:
    runs-on: windows-latest
    needs: [inicio]
    steps:
      - name: texto
        run: echo "Construir"
      - name: Instalar PHP y composer Windows
        run: choco install php --version=8.1
          choco install composer
          php -v
          composer --version
  test:
    runs-on: windows-latest
    needs: [build]
    steps:
      - name: texto
        run: echo "test"

  deploy:
    runs-on: windows-latest
    needs: [test]
    steps:
      - name: texto
        run: echo "desplegar"
